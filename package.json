{
  "devDependencies": {
    "husky": "^3.1.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "(cd web && npm run prettify && npm run typecheck && npm run lint -- --fix && npm run test:unit)"
    }
  }
}
