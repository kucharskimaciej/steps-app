{
  "dependencies": {
    "firestore-backup-restore": "^1.3.1"
  },
  "scripts": {
    "backup": "firestore-backup-restore --accountCredentials ${FIREBASE_CREDENTIALS:-../../firebase.${FIREBASE_ENV:-development}.json} --backupPath ./backups/${FIREBASE_ENV:-development}.$(date \"+%Y%m%d-%H%M%S\")",
    "restore": "firestore-backup-restore --restoreAccountCredentials ${FIREBASE_CREDENTIALS:-../../firebase.${FIREBASE_ENV:-development}.json} --backupPath"
  }
}
